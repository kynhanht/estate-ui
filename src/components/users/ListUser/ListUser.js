import classNames from 'classnames/bind';
import styles from './ListUser.module.scss';

const cx = classNames.bind(styles);
function ListUser() {
    return <div className={cx('')}>ListUser</div>;
}

export default ListUser;
